/**
 * Sliders
 */
jQuery(document).ready(function($) {

  //
  // Slick Slider
  //
  $('.images').slick({
    slidesToShow: 1,
    slidesToScroll: 1,
    fade: false,
    arrows: false,
    dots: true
  });

  //
  // Slick Slider
  //
  $('.images-slider').slick({
    slidesToShow: 1,
    slidesToScroll: 1,
    fade: false,
    arrows: false,
    dots: true
  });

});
